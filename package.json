{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "start": "PORT=3030 node ./.output/server/index.mjs",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "golive": "git push && ssh root@p.jasperstephenson.com 'source ~/.profile && cd ~/playtestkyotowebsite && git pull && nvm use 18 && npm run build && nvm use 16 && pm2 restart playtest && pm2 logs'"
  },
  "devDependencies": {
    "@nuxt/devtools": "^2.7.0",
    "@nuxtjs/i18n": "^9.5.6",
    "nuxt": "^4.4.7"
  },
  "dependencies": {
    "sass": "^1.69.5"
  }
}
